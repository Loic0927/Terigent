import { createAdminServiceItemHandler } from '../../../server/service-handler.js';
import * as repository from '../../../server/service-repository.js';

export default createAdminServiceItemHandler(repository);
